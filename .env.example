# .env.example
# This file contains environment variables used by the Docker Compose setup
# and the Go application. Copy this file to a new file named '.env'
# and fill in your desired values.

# PostgreSQL Database Configuration
# These variables are used to configure the PostgreSQL container.
DB_NAME=scrumer
DB_USER=user
DB_PASSWORD=password

# Application Database Connection (used by backend and seed services)
# These variables are used by the Go application to connect to the PostgreSQL database.
# DB_HOST should be 'db' when running inside Docker Compose.
# DB_PORT is the internal port of the PostgreSQL container.
# DB_SSLMODE typically 'disable' for local development, or 'require'/'verify-full' for production.
# The values for DB_USER, DB_PASSWORD, and DB_NAME are usually the same as above.
# DB_HOST=db
# DB_PORT=5432
# DB_USER=user
# DB_PASSWORD=password
# DB_NAME=scrumer
# DB_SSLMODE=disable
